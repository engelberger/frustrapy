# Runtime dependencies (mirrors [project.dependencies] in pyproject.toml).
# Authoritative source is pyproject.toml; this file is kept for convenience.
numpy
pandas
biopython
plotly
scipy
scikit-learn
python-igraph
leidenalg
tqdm
# Optional (see pyproject [project.optional-dependencies]):
psutil      # perf: memory logging
kaleido     # viz: static image export
py3Dmol>=2.4.2  # viz: 3D structure viewer
