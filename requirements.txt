# Runtime dependencies (mirrors [project.dependencies] in pyproject.toml).
# Authoritative source is pyproject.toml; this file is kept for convenience.
numpy
pandas
biopython
plotly<6          # plotly 6.x removed the `titlefont` property the plot path uses
scipy
scikit-learn
python-igraph
leidenalg
tqdm
rich              # utils/ui.py Rich display, imported by the core engine
matplotlib        # benchmark/plotting.py (seaborn/matplotlib benchmark figures)
seaborn           # benchmark/plotting.py
logomaker         # evolution/sequence_logo.py sequence logos (Phase-7 demotion candidate)
# Optional (see pyproject [project.optional-dependencies]):
psutil      # perf: memory logging
kaleido     # viz: static image export
py3Dmol>=2.4.2  # viz: 3D structure viewer
